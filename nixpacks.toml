[phases.setup]
nixPkgs = ["php80", "php80Packages.composer"]

[phases.build]
cmds = ["composer install --no-dev --ignore-platform-reqs"]

[phases.setup.environment]
PHPRC = "/etc/php80"

[start]
cmd = "php -S 0.0.0.0:$PORT"